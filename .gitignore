# Binaries
*.exe
*.dll
*.so
*.dylib

# Test artifacts
*.test
*.out
coverage.html
coverage.txt

# Vendor
/vendor/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Local env
.env
.env.local
*.local

# Private planning docs (research, design, IMPL/ADR notes) — kept local only,
# not pushed to remote. Strategy-ish material lives in subdirs and is
# ignored by intent. The exception is /docs/protocol/, which is the
# polyglot-facing schema + YAML format reference and MUST be in-repo.
/docs/*
!/docs/protocol/
